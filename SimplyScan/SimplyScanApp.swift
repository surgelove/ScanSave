import SwiftUI

/// The entry point for Simply Scan.
///
/// Launches the main content view wrapped in a `WindowGroup`.
@main
struct SimplyScanApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

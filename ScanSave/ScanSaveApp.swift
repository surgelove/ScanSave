import SwiftUI

/// The entry point for ScanSave.
///
/// Launches the main content view wrapped in a `WindowGroup`.
@main
struct ScanSaveApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
